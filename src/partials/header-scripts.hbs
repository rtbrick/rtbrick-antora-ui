<script>
  function LoadDocViewer(id, file) {
    const path = document.location.pathname.replace(/[^/]*$/, '')
    const src = `/_/vendor/ViewerJS/#${path}/_attachments/${file}`
    document.getElementById(id).src = src
  }
</script>
